export type StreamingDetail = {
    source: string,
}

export type Camera = {
    cameraName: string,
    description: string | null,
    address: string,
    location: string | null,
    cameraId: string
}